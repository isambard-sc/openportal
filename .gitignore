# SPDX-FileCopyrightText: © 2024 Christopher Woods <Christopher.Woods@bristol.ac.uk>
# SPDX-License-Identifier: CC0-1.0

target
.env
Cargo.lock
invitation.toml
invite*.toml
*_config.toml
*-config.toml
example*.toml
slurm_example_data*
slurm_demo_data*
agents/*.toml

# Left behind by running the Python example (python/examples/site_portal)
__pycache__
*.pyc
portal-state

# Everything `python example.py setup` creates (python/examples/site_portal)
python/examples/site_portal/data

# Generated by `cargo run --bin stub_gen` before the wheel is built, which is a
# CI step rather than a checked-in file - it has never been tracked. Ignored so
# that running the generator locally, as you must to check a new getter appears
# in it, cannot put a 76KB build artefact into a commit.
python/openportal.pyi
